#include <stdio.h>
/**
 * main - This is the entry point of our program
 *
 * Return: This returns 0 when success
 */
int main(void)
{
int l = 48;
while (l <= 57)
{
putchar(l);
l = l + 1;
}
putchar('\n');
return (0);
}

